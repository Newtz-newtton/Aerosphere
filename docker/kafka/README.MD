# Kafka Runtime

This directory contains the local Kafka runtime used during AeroSphere development.

Components:

- Apache Kafka (KRaft)
- Kafka UI

The Docker configuration is intended for local development.

Production deployment will be handled separately during the AWS phase.